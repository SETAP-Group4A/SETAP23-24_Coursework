const {validateEmail, validatePassword} = require('./login_signup');

describe("validateEmail", () => {
    test ('Enter a valid email', () => {
        expect(test@test.com). toBe(True);
        )
    )


    describe("validateEmail", () => {
        test ('Enter a valid email', () => {
            expect(123@mail.com). toBe(True);
            )
        )

        describe("validateEmail", () => {
            test ('Enter a valid email', () => {
                expect(notanemail). toBe(False);
                )
            )

            describe("validateEmail", () => {
                test ('Enter a valid email', () => {
                    expect(gmail.com). toBe(True);
                    )
                )


                describe("validatePassword", () => {
                    test ('Enter a valid password', () => {
                        expect(password). toBe(True);
                        )
                    )
                    describe("validatePassword", () => {
                        test ('Enter a valid password', () => {
                            expect(ggggyg). toBe(false);
                            )
                        )


                        describe("validatePassword", () => {
                            test ('Enter a valid password', () => {
                                expect(123). toBe(True);
                                )
                            )